{
  "name": "mo-site-starter",
  "version": "1.0.0",
  "description": "My personal website",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run sass-watch & npm run pug-watch",
    "sass-watch": "sass sass:css --watch --style compressed --no-source-map",
    "pug-watch": "pug index.pug pages -w"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/marcentusch/MO-Personal-Site"
  },
  "author": "Marc Obel",
  "license": "ISC",
  "homepage": "https://marcobel.com",
  "browserslist": [
    "last 2 chrome versions",
    "last 2 safari versions"
  ],
  "devDependencies": {
    "sass": "^1.26.10"
  },
  "dependencies": {
    "pug-cli": "^1.0.0-alpha6"
  }
}
